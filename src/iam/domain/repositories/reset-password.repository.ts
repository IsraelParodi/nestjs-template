import { ResetPassword } from '../reset-password';
import { BaseRepository } from '@common/repositories/BaseRepository';

export abstract class ResetPasswordRepository extends BaseRepository<ResetPassword> {}
